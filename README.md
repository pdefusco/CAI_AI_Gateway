# AI Gateway in Cloudera AI

## Usage

```
curl -X POST http://localhost:8000/model-a \
  -H "Content-Type: application/json" \
  -d '{
        "inputs": "What is a finite state machine?"
      }'

```
